apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.global.healthDb.name }}-headless
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.global.healthDb.name }}
    app.kubernetes.io/name: {{ .Values.global.healthDb.name }}
    app.kubernetes.io/part-of: meditrack-db
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: {{ .Values.global.healthDb.name }}
  ports:
    - port: 5432
      targetPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.global.healthDb.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.global.healthDb.name }}
    app.kubernetes.io/name: {{ .Values.global.healthDb.name }}
    app.kubernetes.io/part-of: meditrack-db
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.global.healthDb.name }}
  ports:
    - port: 5432
      targetPort: 5432
