apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Values.global.healthDb.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.global.healthDb.name }}
    app.kubernetes.io/name: {{ .Values.global.healthDb.name }}
    app.kubernetes.io/part-of: meditrack-db
spec:
  replicas: 1
  serviceName: {{ .Values.global.healthDb.name }}-headless
  podManagementPolicy: OrderedReady
  selector:
    matchLabels:
      app: {{ .Values.global.healthDb.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.global.healthDb.name }}
        app.kubernetes.io/name: {{ .Values.global.healthDb.name }}
        app.kubernetes.io/part-of: meditrack-db
    spec:
      serviceAccountName: meditrack-db-sa
      terminationGracePeriodSeconds: 60
      securityContext:
        fsGroup: 999
        runAsUser: 999
        runAsGroup: 999
      initContainers:
        - name: init-permissions
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              mkdir -p /var/lib/postgresql/data/pgdata
              chown -R 999:999 /var/lib/postgresql/data
              chmod 700 /var/lib/postgresql/data/pgdata
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          securityContext:
            runAsUser: 0
            runAsNonRoot: false
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 50m
              memory: 128Mi
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
              protocol: TCP
          env:
            - name: POSTGRES_DB
              value: {{ .Values.global.healthDb.dbName }}
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: meditrack-db-secrets
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: meditrack-db-secrets
                  key: POSTGRES_PASSWORD
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              cpu: {{ .Values.global.healthDb.resources.requests.cpu }}
              memory: {{ .Values.global.healthDb.resources.requests.memory }}
            limits:
              cpu: {{ .Values.global.healthDb.resources.limits.cpu }}
              memory: {{ .Values.global.healthDb.resources.limits.memory }}
          livenessProbe:
            exec:
              command: ["sh", "-c", "pg_isready -U $POSTGRES_USER -d $POSTGRES_DB"]
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            exec:
              command: ["sh", "-c", "pg_isready -U $POSTGRES_USER -d $POSTGRES_DB"]
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          {{- include "meditrack.containerSecurityContext" . | nindent 10 }}
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        storageClassName: {{ .Values.global.nfs.storageClass }}
        accessModes: [ReadWriteOnce]
        resources:
          requests:
            storage: {{ .Values.global.healthDb.storage }}
