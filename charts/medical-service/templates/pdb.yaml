{{- if .Values.global.medicalService.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ .Values.global.medicalService.name }}-pdb
  namespace: {{ .Release.Namespace }}
spec:
  minAvailable: {{ .Values.global.medicalService.pdb.minAvailable }}
  selector:
    matchLabels:
      app: {{ .Values.global.medicalService.name }}
{{- end }}
