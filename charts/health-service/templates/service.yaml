apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.global.healthService.name }}
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.global.healthService.name }}
  ports:
    - port: {{ .Values.global.healthService.port }}
      targetPort: {{ .Values.global.healthService.port }}
