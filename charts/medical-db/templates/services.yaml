apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.global.medicalDb.name }}-headless
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.global.medicalDb.name }}
    app.kubernetes.io/name: {{ .Values.global.medicalDb.name }}
    app.kubernetes.io/part-of: meditrack-db
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: {{ .Values.global.medicalDb.name }}
  ports:
    - port: 5432
      targetPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.global.medicalDb.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.global.medicalDb.name }}
    app.kubernetes.io/name: {{ .Values.global.medicalDb.name }}
    app.kubernetes.io/part-of: meditrack-db
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.global.medicalDb.name }}
  ports:
    - port: 5432
      targetPort: 5432
