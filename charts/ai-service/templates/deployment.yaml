apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.global.aiService.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.global.aiService.name }}
    app.kubernetes.io/name: {{ .Values.global.aiService.name }}
    app.kubernetes.io/part-of: meditrack
    app.kubernetes.io/managed-by: helm
    environment: {{ .Values.global.environment }}
spec:
  replicas: {{ .Values.global.aiService.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.global.aiService.name }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: {{ .Values.global.aiService.name }}
        app.kubernetes.io/name: {{ .Values.global.aiService.name }}
        app.kubernetes.io/part-of: meditrack
        environment: {{ .Values.global.environment }}
    spec:
      serviceAccountName: meditrack-app-sa
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: {{ .Values.global.aiService.name }}
                topologyKey: kubernetes.io/hostname
      initContainers:
        {{- include "meditrack.initWait" (dict "name" "redis" "port" "6379") | nindent 8 }}

      containers:
        - name: {{ .Values.global.aiService.name }}
          image: {{ .Values.global.registry }}/{{ .Values.global.aiService.image }}:{{ .Values.global.imageTag }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.global.aiService.port }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: meditrack-config
          env:
            - name: PORT
              value: {{ .Values.global.aiService.port | quote }}
            - name: GEMINI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: meditrack-secrets
                  key: GEMINI_API_KEY
            - name: GROQ_API_KEY
              valueFrom:
                secretKeyRef:
                  name: meditrack-secrets
                  key: GROQ_API_KEY
            - name: HUGGINGFACE_API_KEY
              valueFrom:
                secretKeyRef:
                  name: meditrack-secrets
                  key: HUGGINGFACE_API_KEY
          resources:
            requests:
              cpu: {{ .Values.global.aiService.resources.requests.cpu }}
              memory: {{ .Values.global.aiService.resources.requests.memory }}
            limits:
              cpu: {{ .Values.global.aiService.resources.limits.cpu }}
              memory: {{ .Values.global.aiService.resources.limits.memory }}
          {{- include "meditrack.appProbes" (dict "port" .Values.global.aiService.port) | nindent 10 }}
          {{- include "meditrack.lifecycle" . | nindent 10 }}
          {{- include "meditrack.containerSecurityContext" . | nindent 10 }}
